'use strict';
window.toggleTopic=function(button){const panel=document.getElementById(button.getAttribute('aria-controls'));const open=button.getAttribute('aria-expanded')!=='true';const card=button.closest('.module-card');card.querySelectorAll('.topic-toggle').forEach(b=>{b.setAttribute('aria-expanded','false');document.getElementById(b.getAttribute('aria-controls')).classList.remove('active')});button.setAttribute('aria-expanded',String(open));panel.classList.toggle('active',open)};
let previewTrigger;
window.openModal=function(url,title){const dialog=document.getElementById('module-preview');previewTrigger=document.activeElement;dialog.querySelector('iframe').src=url;dialog.querySelector('iframe').title=title;document.getElementById('preview-title').textContent=title;dialog.showModal()};
window.closeModal=function(){const d=document.getElementById('module-preview');d.close();d.querySelector('iframe').src='about:blank';if(previewTrigger)previewTrigger.focus()};
document.addEventListener('DOMContentLoaded',()=>{document.documentElement.classList.add('has-course-js');if(!window.INEMA)return;INEMA.init();document.getElementById('theme-toggle').addEventListener('click',()=>INEMA.setPref('theme',document.documentElement.classList.contains('dark')?'claro':'inema-dark'));document.querySelectorAll('[data-continue]').forEach(b=>b.addEventListener('click',()=>{if(!INEMA.resume())location.href=b.dataset.continue}));document.querySelectorAll('[data-copy]').forEach(b=>b.addEventListener('click',async()=>{const text=document.getElementById(b.dataset.copy).textContent;try{await navigator.clipboard.writeText(text);b.textContent="Copied"}catch{const sel=window.getSelection();const r=document.createRange();r.selectNodeContents(document.getElementById(b.dataset.copy));sel.removeAllRanges();sel.addRange(r);b.textContent="Text selected"}setTimeout(()=>b.textContent="Copy",2000)}));const quiz=document.querySelector('[data-quiz-definition]');if(quiz){const q=JSON.parse(quiz.textContent);INEMA.registerCheck(q.id,q);const current=JSON.parse(INEMA.exportJSON()).checks[q.id];if(current)INEMA.submitCheck(q.id,current.choice)}document.addEventListener('keydown',e=>{if(e.key==='Escape'){const panel=document.querySelector('[data-inema-appearance]');if(panel&&panel.dataset.open==='true'){panel.dataset.open='false';const b=document.querySelector('[data-inema-appearance-toggle]');b.setAttribute('aria-expanded','false');b.focus()}}});document.addEventListener('click',e=>{const panel=document.querySelector('[data-inema-appearance]');if(panel&&!panel.contains(e.target)&&!e.target.closest('[data-inema-appearance-toggle]')){panel.dataset.open='false';document.querySelector('[data-inema-appearance-toggle]').setAttribute('aria-expanded','false')}});window.addEventListener('storage',()=>INEMA.init());const dialog=document.getElementById('module-preview');if(dialog){dialog.addEventListener('cancel',e=>{e.preventDefault();closeModal()});dialog.addEventListener('click',e=>{if(e.target===dialog)closeModal()})}});
